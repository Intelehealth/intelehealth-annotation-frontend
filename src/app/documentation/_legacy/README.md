# Legacy documentation (internal)

The original single-page spec docs: platform overview, functional requirements,
success metrics, UI/UX design, assumptions, dependencies. Internal material, not
routed. The user-facing docs live in `../_docs` and `../[slug]`.
